import { Button } from '@mantine/core';
import React from 'react';
import './Layout.css';

export const Layout: React.FC = () => {
  return (
    <article>
      <Button>test</Button>
    </article>
  );
};
